__pycache__/
*.pyc
.venv/
.env
data/*
!data/.gitkeep
*.db
.ruff_cache/
.pytest_cache/

# Local tooling / artifacts
.claude/settings.local.json
*.zip
.DS_Store

# pirated source PDFs — never commit (see memory)
books/

# AnkiWeb .apkg build inputs — wordlist reference only, not redistributed (see
# docs/anki-vocab-plan.md). Only the reviewed YAML output is committed.
imports/
