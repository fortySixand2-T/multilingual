# Reverse proxy + automatic HTTPS. Set SITE_ADDRESS in .env:
#   SITE_ADDRESS=tef.example.com   -> Caddy gets a real cert automatically
#   (unset)                        -> serves plain HTTP on :80 (LAN / testing)
{$SITE_ADDRESS::80} {
	encode gzip
	reverse_proxy app:9000
}
